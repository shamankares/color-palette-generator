{
  "name": "color-palette-k-means-frontend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "parcel build index.html --public-url / --dist-dir dist",
    "preview": "parcel index.html --public-url /",
    "load-env": "node -r dotenv/config",
    "dev": "npm-run-all load-env preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
